[pytest]
addopts = -ra --strict-markers
testpaths = .
markers =
    mutating: changes the canonical synthetic dataset and must run after read-only checks
    visual: compares a stable surface with the committed Chromium baseline
    matrix: exercises a viewport, effective zoom, theme, or reduced-motion variant
